# Spec-Kit tasks.md vs Task-Master：定位区别详解

**核心原则**：各司其职，相互补充，不重合、不冲突

---

## 一、功能边界对比

### 📐 Spec-Kit 的 tasks.md：定义"怎么做"（How）

**性质**：**静态设计文档**（Design Document）

**核心职责**：
```
"这个功能如何实施？"
```

**包含内容**：
- ✅ 技术实施细节（文件路径、函数名、类名）
- ✅ 具体的代码示例
- ✅ 测试用例描述
- ✅ 数据模型定义
- ✅ API 端点规格
- ✅ 提交信息格式（`git commit -m "..."`）

**示例**：
```markdown
- [ ] T004 [P] 创建学习记录扩展模型（LearningRecord）
  - 在 `backend/app/models/learning.py` 实现 LearningRecord 类
  - 字段：id, student_id, question_content, student_answer, is_correct, time_spent
  - 添加索引：idx_student_id, idx_is_correct, idx_created_at
  - 实现 SQLAlchemy 模型定义
  - 提交：`git commit -m "[LWP-2.2-T004] feat: 扩展学习记录模型"`
```

**特点**：
- 📄 **文档导向**：是人阅读的设计文档
- 🔧 **技术细节**：包含文件路径、函数签名等
- 📝 **不可执行**：是计划，不是执行状态
- 🔗 **规范绑定**：与 spec.md 紧密关联

---

### ⏱️ Task-Master：追踪"什么时候做"（When）

**性质**：**动态执行状态**（Execution Status）

**核心职责**：
```
"这个任务现在进展如何？"
```

**包含内容**：
- ✅ 任务状态（pending → in-progress → done）
- ✅ 优先级（P0, P1, P2）
- ✅ 分配情况（claimed_by: backend-dev-1）
- ✅ 时间追踪（created_at, started_at, completed_at）
- ✅ 依赖关系（T005 依赖 T004）
- ✅ 进度统计（完成百分比）

**示例**：
```json
{
  "id": "LWP-2.2-T004",
  "title": "创建学习记录扩展模型",
  "status": "in-progress",
  "priority": "critical",
  "claimed_by": "backend-dev-1",
  "claimed_at": "2025-01-15T10:30:00Z",
  "dependencies": ["LWP-2.2-T003"],
  "progress": 50,
  "estimated_hours": 2,
  "time_spent": 1
}
```

**特点**：
- ⚡ **状态驱动**：反映任务的实时状态
- 👥 **协作导向**：支持多 Agent 并行工作
- 🔢 **可量化**：进度、时间、完成度
- 🔌 **系统集成**：与 Hamster、GitHub 等外部工具集成

---

## 二、维度对比表

| 维度 | Spec-Kit tasks.md | Task-Master |
|------|-------------------|-------------|
| **本质** | 设计文档 | 执行状态 |
| **回答问题** | 怎么做？（How） | 什么时候做？（When） |
| **生命周期** | 创建后不变（除非重新设计） | 持续变化（pending → done） |
| **受众** | 开发者（阅读实施细节） | Agent/PM（追踪进度） |
| **更新频率** | 低（设计变更时） | 高（状态实时变化） |
| **技术细节** | ✅ 包含文件路径、代码示例 | ❌ 只关注状态和进度 |
| **依赖关系** | ✅ 逻辑依赖（T005 需要 T004 完成） | ✅ 可执行依赖（自动检查状态） |
| **分配管理** | ❌ 不包含 | ✅ claimed_by, claimed_at |
| **时间追踪** | ❌ 不包含 | ✅ estimated_hours, time_spent |
| **外部集成** | ❌ 无 | ✅ Hamster, GitHub Issues |
| **自动化** | ❌ 手动编写 | ✅ Agent 可以领取/更新 |

---

## 三、具体案例对比

### 场景：实现学习记录功能

#### Spec-Kit tasks.md 会写：

```markdown
## Phase 1: 数据模型

- [ ] T004 创建学习记录扩展模型（LearningRecord）
  - **文件位置**: `backend/app/models/learning.py`
  - **技术实现**:
    ```python
    class LearningRecord(Base):
        __tablename__ = 'learning_records'

        id = Column(Integer, primary_key=True)
        student_id = Column(Integer, ForeignKey('students.id'))
        question_content = Column(Text, nullable=False)
        student_answer = Column(String(500))
        is_correct = Column(Boolean, default=False)
        time_spent = Column(Integer)  # 秒
        created_at = Column(DateTime, default=datetime.utcnow)

        __table_args__ = (
            Index('idx_student_id', 'student_id'),
            Index('idx_is_correct', 'is_correct'),
        )
    ```
  - **测试要求**:
    - 测试创建记录功能
    - 测试索引是否生效
    - 测试外键约束
  - **提交格式**:
    ```
    [LWP-2.2-T004] feat: 扩展学习记录模型

    - 实现 LearningRecord SQLAlchemy 模型
    - 添加 student_id, question_content 等字段
    - 创建索引优化查询性能

    Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
    ```
```

**重点**：告诉开发者**如何**实现这个功能。

---

#### Task-Master 会记录：

```json
{
  "id": "LWP-2.2-T004",
  "title": "创建学习记录扩展模型",
  "status": "in-progress",
  "priority": "critical",
  "dependencies": ["LWP-2.2-T003"],  // 必须先完成数据加密服务
  "claimed_by": "backend-dev-1",     // 被哪个 Agent 领取
  "claimed_at": "2025-01-15T10:30:00Z",
  "estimated_hours": 2,              // 预估 2 小时
  "time_spent": 1.5,                 // 已用 1.5 小时
  "progress": 75,                    // 完成度 75%
  "blockers": [],                    // 无阻塞
  "tags": ["backend", "database", "phase-1"],
  "related_spec": "001-learning-management/spec.md",
  "related_task": "T004"
}
```

**重点**：告诉 PM/Agent**当前进展如何**。

---

## 四、为什么需要两个系统？

### ❌ 如果只有 Spec-Kit tasks.md：

**问题**：
1. 无法追踪实时状态（任务是否被领取？当前进度？）
2. 无法支持多 Agent 并行（不知道哪个 Agent 在做什么）
3. 无法自动管理依赖（T005 不知道 T004 是否完成）
4. 无法与外部工具集成（Hamster, GitHub Issues）
5. 无法量化工作量（预估时间 vs 实际时间）

**场景**：
```
@backend-dev-1: 我该做什么？
🤔 不知道，tasks.md 只说要做 T004，但不知道 T004 被谁领取了

@pm: 进度如何？
🤔 不知道，tasks.md 是静态文档，没有状态信息
```

---

### ❌ 如果只有 Task-Master：

**问题**：
1. 缺少技术实施细节（文件路径？函数名？）
2. Agent 不知道**如何**实现（只有标题，没有代码示例）
3. 提交格式不统一（commit message 风格混乱）
4. 测试要求不明确（测什么？怎么测？）
5. 违反"规范先于代码"原则

**场景**：
```
@backend-dev-1: 领取了 T004，开始实现
🤔 等等，文件应该创建在哪？字段有哪些？
❌ 只能自己猜测，违反规范驱动开发
```

---

### ✅ 两者结合：最佳实践

```
┌─────────────────────────────────────────────────────────────┐
│                    完整的开发流程                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1️⃣ 规范阶段                          │
│     • 定义 API 规格                                          │
│     • 编写 tasks.md（详细实施步骤）                          │
│     • 包含：文件路径、代码示例、测试要求、提交格式           │
│                                                              │
│  2️⃣ 任务创建（Task-Master）                                   │
│     • 从 tasks.md 导入任务到 Task-Master                    │
│     • 设置：优先级、依赖关系、预估时间                       │
│     • 状态：pending                                          │
│                                                              │
│  3️⃣ 任务领取（Agent）                                           │
│     • Agent 查询：tm list --status=pending --unclaimed      │
│     • 领取任务：tm claim LWP-2.2-T004 --agent=backend-dev-1 │
│     • 状态变更为：in-progress                                │
│                                                              │
│  4️⃣ 执行实施（Agent + Spec-Kit）                              │
│     • Agent 阅读：specs/001-learning-management/tasks.md    │
│     • 按照技术细节实现（文件路径、代码示例）                 │
│     • 遵循 TDD 循环：Red → Green → Refactor                 │
│     • 按照格式提交：git commit -m "[LWP-2.2-T004] feat..."  │
│                                                              │
│  5️⃣ 完成任务（Task-Master）                                   │
│     • Agent 更新：tm set-status LWP-2.2-T004 done           │
│     • 释放锁：backend-dev-1 → available                    │
│     • 同步到 Hamster（可选）                                 │
│                                                              │
│  6️⃣ 验证合规（Spec-Kit）                                       │
│     • 运行测试：pytest                                       │
│     • 检查覆盖率：≥ 80%                                      │
│     • 验证与规范一致                                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、核心原则总结

### Spec-Kit tasks.md：**设计蓝图**

```
类似建筑图纸：
- 详细记录每个细节（尺寸、材料、工艺）
- 一旦确定，原则上不变
- 指导施工人员"怎么做"
```

### Task-Master：**施工进度表**

```
类似工程管理：
- 记录每个环节的状态（已开工、进行中、已完工）
- 实时更新，动态变化
- 管理资源分配（谁在做什么）
- 协调多个施工队（多 Agent 并行）
```

### 协作关系：

```
Spec-Kit tasks.md (静态设计)
         ↓
    导入任务
         ↓
Task-Master (动态执行)
         ↓
   Agent 领取任务
         ↓
  参考 tasks.md 实施
         ↓
  更新 Task-Master 状态
         ↓
      完成任务
```

---

## 六、类比说明

### 📖 类比：做一道菜

| 角色 | Spec-Kit tasks.md | Task-Master |
|------|-------------------|-------------|
| **菜谱** | "第 3 步：切洋葱，厚度 2mm" | "第 3 步已完成 ✅" |
| **厨师** | 参考菜谱学习做法 | 记录哪些步骤已完成 |
| **餐厅经理** | 不看菜谱 | 查看订单进度 |

**关键点**：
- 菜谱（Spec-Kit）告诉你**怎么做**
- 订单系统（Task-Master）告诉你**当前进展**
- 两者缺一不可！

---

## 七、项目宪章的要求

根据项目宪章（constitution.md）：

```
**关键原则**：
- ✅ 规范先行：先有规范，后有任务
- ✅ 单一职责：各司其职，功能不重合
- ✅ 相互引用：规范引用任务，任务引用规范
- ✅ 保持同步：变更时更新双方
```

**强制要求**：
1. ❌ **禁止**：只有 Task-Master，没有 Spec-Kit
2. ❌ **禁止**：Spec-Kit 和 Task-Master 内容不一致
3. ✅ **必须**：Task-Master 任务引用对应的 spec_id
4. ✅ **必须**：规范变更时同步更新 Task-Master

---

## 八、常见问题 FAQ

### Q1: 为什么不把 tasks.md 的内容直接放到 Task-Master 的 description 字段？

**A**: 因为：
- Task-Master 的 description 是**摘要**，不适合放大量代码
- tasks.md 是**版本控制的设计文档**，需要独立维护
- Agent 需要详细的**可读的文档**，而不是 JSON 字段

### Q2: 如果 tasks.md 变更了，Task-Master 怎么同步？

**A**:
1. 小变更：直接更新 Task-Master 的 description
2. 大变更：重新导入任务，Task-Master 会保留状态
3. 删除任务：标记为 cancelled，不直接删除（保留历史）

### Q3: Agent 如何使用这两个系统？

**A**:
```bash
# 1. Agent 领取任务（Task-Master）
tm claim LWP-2.2-T004 --agent=backend-dev-1

# 2. Agent 阅读实施细节
cat specs/001-learning-management/tasks.md | grep -A 20 "T004"

# 3. Agent 按照规范实施
# （创建文件、编写代码、运行测试）

# 4. Agent 更新状态
tm set-status LWP-2.2-T004 done
```

---

## 总结

| 系统 | 定位 | 价值 | 不可替代的原因 |
|------|------|------|---------------|
| **Spec-Kit tasks.md** | 设计文档 | 告诉开发者"怎么做" | 包含技术细节，指导实施 |
| **Task-Master** | 执行状态 | 告诉 PM/Agent"进展如何" | 支持多 Agent 并行，状态追踪 |

**两者结合**：实现规范驱动开发 + 自动化任务管理的完美闭环。

---

**作者**: Claude Sonnet 4.5
**版本**: 1.0
**日期**: 2025-01-15
